/**
 * PipelineForge Deployment Services Catalog — an in-process SOA exercise: business-aligned
 * services (Deployment Approval, Environment Provisioning) that independent callers discover
 * and reuse through a stable, versioned Service Catalog rather than constructing directly.
 */
package com.isaqb.practice.soa;
