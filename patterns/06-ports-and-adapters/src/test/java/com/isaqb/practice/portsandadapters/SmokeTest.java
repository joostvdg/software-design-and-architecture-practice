package com.isaqb.practice.portsandadapters;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/**
 * Placeholder so {@code mvn verify} is green from a clean checkout, before milestone 1
 * adds the core domain model and ports. Delete once the core has its own tests.
 */
class SmokeTest {

  @Test
  void moduleIsWired() {
    assertTrue(true);
  }
}
