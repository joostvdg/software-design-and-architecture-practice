package com.isaqb.practice.layers;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/**
 * Placeholder so {@code mvn verify} is green from a clean checkout, before milestone 1
 * adds the domain layer. Delete once the domain layer has its own tests.
 */
class SmokeTest {

  @Test
  void moduleIsWired() {
    assertTrue(true);
  }
}
