package com.isaqb.practice.pipesandfilters;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/**
 * Placeholder so {@code mvn verify} is green from a clean checkout, before milestone 1
 * adds the first filter. Delete once the pipeline package has its own tests.
 */
class SmokeTest {

  @Test
  void moduleIsWired() {
    assertTrue(true);
  }
}
