package com.isaqb.practice.broker;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/**
 * Placeholder so {@code mvn verify} is green from a clean checkout, before milestone 2
 * adds the broker itself and its own tests. Delete once {@code InMemoryBrokerTest}
 * exists.
 */
class SmokeTest {

  @Test
  void moduleIsWired() {
    assertTrue(true);
  }
}
